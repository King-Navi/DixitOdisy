<Window x:Class="WpfCliente.GUI.MostrarCartaModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCliente.GUI"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d"
        d:Title="MostrarCartaModelWindow" Height="451" Width="334" >
    <Grid>
        <!-- Definir la imagen primero para que esté en el fondo -->
        <Image x:Name="imagenElegida" Width="300" ></Image>
        <TextBox x:Name="textBoxPista" Height="50" Margin="32,364,114,21" />
        <Button Content="Selecionar" x:Name="buttonSeleccionar" Height="50" Margin="220,364,46,21" Click="ClicButtonEnviarPista" />
        <Button Background="Transparent"  HorizontalAlignment="Right"  VerticalAlignment="Top" Margin="0,20,20,0" Click="ClicButtonCerrar" >
            <Button.Clip>
                <EllipseGeometry Center="50,50" RadiusX="50" RadiusY="50"/>
            </Button.Clip>
            <Image Source="/Recursos/cerrarCruz.png" Stretch="Uniform" Width="80" HorizontalAlignment="Center" VerticalAlignment="Center"></Image>
        </Button>
    </Grid>
</Window>
