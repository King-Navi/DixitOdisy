<UserControl x:Class="WpfCliente.GUI.MazoUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfCliente.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Aqua">
        <ItemsControl x:Name="itemsControlMazo" ItemsSource="{Binding ImagenCartas, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <!-- Imagen con botón transparente para captar clic -->
                        <Button Command="{Binding ImagenClickCommand}" CommandParameter="{Binding}">
                            <Image Width="100" Height="100" Source="{Binding BitmapImagen}" />
                        </Button>
                        <TextBlock Text="{Binding IdImagen}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
